# Wikimedia requires a User-Agent naming the tool and a contact address.
# In CI this is set from the repository variable of the same name.
UNSC_API_USER_AGENT="unsc-api/1.0 (https://github.com/<you>/unsc-api; you@example.com)"
